package aas

// preprocessor - more like reprocessor but whatever
// modifies statement chain - expands .inc, .def, and .mac and expands instruction aliasing

preproc :: proc(token_chain: ^[dynamic]statement) {


}